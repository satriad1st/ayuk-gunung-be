services:
  mongo:
    image: mongo:7
    container_name: ayuk-gunung-mongo
    restart: unless-stopped
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_DATABASE: ayuk-gunung
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
